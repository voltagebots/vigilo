FROM python:3.12-slim
WORKDIR /app
COPY harness/webhook_listener.py .
EXPOSE 8090
CMD ["python3", "-c", "from webhook_listener import WebhookListener; import time; l = WebhookListener(port=8090); l.start(); time.sleep(1e9)"]
